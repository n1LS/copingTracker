# Build Guide (WIP)

- have a toolchain-rp2040.cmake 

git clone
git submodule --init --recursive
./build.sh

`build.sh` will spit out a bunch of missing declarations and parameters at first run. Set those environment variables and you should be good to go.